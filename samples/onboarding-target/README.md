# Onboarding Target

Small sample project for AI Coder project onboarding smoke tests.

## Commands

```bash
npm test
```

## Notes

The project intentionally starts with an existing `CLAUDE.md` so onboarding can verify incremental
updates instead of full replacement.
