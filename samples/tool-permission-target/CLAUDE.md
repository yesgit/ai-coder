# Tool Permission Target

## Compact Summary

This is a tiny JavaScript fixture used to validate AI Coder tool permission handling. The safe test command is `npm test`. The intended editable file is `src/message.js`.

## Project Overview

The project exports a single message formatter and tests it with Node's built-in test runner.

## Common Commands

- `npm test`: run the fixture test suite.

## Safety Rules

- Do not read or modify files outside this project directory.
- Ask for host approval before running shell commands.
- Ask for host approval before editing files.

